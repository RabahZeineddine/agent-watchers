/**
 * Espaco reservado da configuracao, que a story M4a.7 preenche com provedores,
 * tabela de substituicao de modelo, servidores MCP e orcamentos.
 *
 * Quando ela existir, segredo nenhum aparece aqui: so a referencia guardada e
 * se o cofre tem valor para ela.
 */
export function Configuracao() {
  return (
    <p className="text-muted-foreground text-sm">
      Provedores, substituicao de modelo, servidores MCP e orcamentos entram na
      proxima leva.
    </p>
  );
}
