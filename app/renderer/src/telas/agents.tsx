/**
 * Espaco reservado da tela de agents, que a story M4a.6 preenche com a lista,
 * o historico de versoes e a comparacao entre duas versoes do spec.
 */
export function Agents() {
  return (
    <p className="text-muted-foreground text-sm">
      Lista de agents, historico de versoes e comparacao de spec entram na
      proxima leva.
    </p>
  );
}
